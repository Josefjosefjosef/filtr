# Update articles data — PR-based flow for protected main.
# Never pushes to main. Uses automation branch + single PR + safe auto-merge when data-only.

name: Update articles data

concurrency:
  group: update-articles-data
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write
  actions: write

on:
  workflow_dispatch: {}
  schedule:
    - cron: "*/11 * * * *"

env:
  AUTOMATION_BRANCH: automation/update-articles-data
  PR_TITLE: "chore(data): update articles data"

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Sync main ref (avoid stale checkout on schedule)
        run: |
          set -euo pipefail
          git fetch origin main
          git checkout main
          git reset --hard origin/main

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: scripts/requirements.txt

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/requirements.txt

      - name: Build articles JSON
        env:
          RETENTION_DAYS: "45"
        run: python scripts/build_articles.py

      - name: Build videos JSON (optional)
        shell: bash
        run: |
          set -euo pipefail
          if [ -f "scripts/build_videos.py" ]; then
            python scripts/build_videos.py
            echo "OK: build_videos.py ran"
          else
            echo "SKIP: scripts/build_videos.py not found"
          fi

      - name: Normalize output paths
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p projects/data

          if [ ! -f projects/data/articles.json ]; then
            echo "ERROR: projects/data/articles.json missing"
            exit 1
          fi

          echo "probe-ok $(date --utc +%Y-%m-%dT%H:%M:%SZ)" > projects/data/_probe.txt

          test -s projects/data/articles.json
          if [ "$(stat -c %s projects/data/articles.json)" -eq 0 ]; then
            echo "ERROR: projects/data/articles.json is empty"
            exit 1
          fi

          if [ -f projects/data/videos.json ] && [ "$(stat -c %s projects/data/videos.json)" -eq 0 ]; then
            echo "ERROR: projects/data/videos.json is empty"
            exit 1
          fi

      - name: Capture build generatedAt (for same-cycle deploy verify)
        id: capture_ts
        run: |
          BUILD_GENERATED_AT=$(jq -r '.generatedAt // empty' projects/data/articles.json)
          echo "generated_at=$BUILD_GENERATED_AT" >> "$GITHUB_OUTPUT"
          echo "[BUILD] generatedAt=$BUILD_GENERATED_AT"

      - name: No-diff gate (skip commit/push/PR if no data changes)
        id: no_diff
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet projects/data; then
            { echo "data_changed=false"; echo "commit_skipped=true"; echo "push_skipped=true"; echo "pr_skipped=true"; } >> "$GITHUB_OUTPUT"
            echo "[DATA] No diff in projects/data — PASS, no commit/push/PR"
            exit 0
          fi
          { echo "data_changed=true"; echo "commit_skipped=false"; echo "push_skipped=false"; echo "pr_skipped=false"; } >> "$GITHUB_OUTPUT"
          echo "[DATA] Diff detected — will commit to ${{ env.AUTOMATION_BRANCH }} and open/update PR"

      - name: Commit to automation branch and push
        if: steps.no_diff.outputs.data_changed == 'true'
        id: commit_push
        shell: bash
        run: |
          set -euo pipefail
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add projects/data
          git diff --cached --quiet && { echo "[DATA] Nothing to commit after add"; exit 0; }

          git stash push -m "data" -- projects/data
          git fetch origin main
          git fetch origin "${{ env.AUTOMATION_BRANCH }}" 2>/dev/null || true
          git checkout -B "${{ env.AUTOMATION_BRANCH }}" origin/main
          git stash pop || true
          git add projects/data
          git diff --cached --quiet && { echo "[DATA] Nothing to commit after stash pop"; exit 0; }

          git commit -m "${{ env.PR_TITLE }}"
          # Force push: automation branch is bot-owned; --force-with-lease can block when remote diverged after main-only merges (PR stuck BEHIND).
          git push --force origin "HEAD:${{ env.AUTOMATION_BRANCH }}"
          echo "pushed=true" >> "$GITHUB_OUTPUT"

      - name: Dispatch required checks (GITHUB_TOKEN push does not start push-triggered workflows)
        if: steps.no_diff.outputs.data_changed == 'true' && steps.commit_push.outputs.pushed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          REF="${{ env.AUTOMATION_BRANCH }}"
          REPO="${{ github.repository }}"
          gh workflow run smoke.yml --ref "$REF" --repo "$REPO"
          gh workflow run layout-guard.yml --ref "$REF" --repo "$REPO"
          gh workflow run repo-guard.yml --ref "$REF" --repo "$REPO"
          echo "[CHECKS] Dispatched smoke, layout-guard, repo-guard on $REF"

      - name: Create or update PR
        if: steps.no_diff.outputs.data_changed == 'true' && steps.commit_push.outputs.pushed == 'true'
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          BRANCH="${{ env.AUTOMATION_BRANCH }}"
          EXISTING=$(gh pr list --head "$BRANCH" --base main --state open --json number -q '.[0].number' 2>/dev/null || true)
          if [ -n "$EXISTING" ]; then
            echo "pr_number=$EXISTING" >> "$GITHUB_OUTPUT"
            echo "pr_url=https://github.com/${{ github.repository }}/pull/$EXISTING" >> "$GITHUB_OUTPUT"
            echo "[PR] Updated existing PR #$EXISTING"
            exit 0
          fi
          gh pr create --base main --head "$BRANCH" \
            --title "${{ env.PR_TITLE }}" \
            --body "Auto-update from Update articles data workflow. Data-only change."
          NUM=$(gh pr list --head "$BRANCH" --base main --state open --json number -q '.[0].number')
          echo "pr_number=$NUM" >> "$GITHUB_OUTPUT"
          echo "pr_url=https://github.com/${{ github.repository }}/pull/$NUM" >> "$GITHUB_OUTPUT"
          echo "[PR] Created PR #$NUM"

      - name: Merge latest main into PR head if behind (unblock auto-merge)
        if: steps.no_diff.outputs.data_changed == 'true' && steps.commit_push.outputs.pushed == 'true'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          NUM="${{ steps.pr.outputs.pr_number }}"
          [ -z "$NUM" ] && exit 0
          echo "[SYNC] PUT update-branch for PR #$NUM"
          gh api --method PUT "repos/${{ github.repository }}/pulls/${NUM}/update-branch" || echo "[SYNC] update-branch skipped or failed (non-fatal)"

      - name: Diff scope check (data-only → allow auto-merge)
        if: steps.no_diff.outputs.data_changed == 'true' && steps.commit_push.outputs.pushed == 'true'
        id: scope
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          CHANGED=$(git diff --name-only origin/main...HEAD)
          echo "[SCOPE] Changed files:"
          echo "$CHANGED"
          DATA_ONLY=true
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            case "$f" in
              projects/data/*) ;;
              *) DATA_ONLY=false; break ;;
            esac
          done <<< "$CHANGED"
          if [ "$DATA_ONLY" = true ]; then
            echo "data_only=true" >> "$GITHUB_OUTPUT"
            echo "[SCOPE] data-only — auto-merge allowed"
          else
            echo "data_only=false" >> "$GITHUB_OUTPUT"
            echo "[SCOPE] non-data change — auto-merge disabled"
          fi

      - name: Enable auto-merge (squash) when data-only
        if: steps.no_diff.outputs.data_changed == 'true' && steps.commit_push.outputs.pushed == 'true' && steps.scope.outputs.data_only == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          NUM="${{ steps.pr.outputs.pr_number }}"
          [ -z "$NUM" ] && exit 0
          gh pr merge "$NUM" --auto --squash
          echo "[AUTO-MERGE] Enabled for PR #$NUM"

      - name: Merge PR same-cycle (data-only, no hanging PR)
        id: merge_now
        if: steps.no_diff.outputs.data_changed == 'true' && steps.commit_push.outputs.pushed == 'true' && steps.scope.outputs.data_only == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          NUM="${{ steps.pr.outputs.pr_number }}"
          [ -z "$NUM" ] && exit 0
          if gh pr merge "$NUM" --admin --merge 2>/dev/null; then
            echo "merged_now=true" >> "$GITHUB_OUTPUT"
            echo "[SAME-CYCLE] PR #$NUM merged via --admin --merge"
          else
            echo "merged_now=false" >> "$GITHUB_OUTPUT"
            echo "[SAME-CYCLE] Relying on auto-merge; will wait for merge"
          fi

      - name: Wait for PR merge (when not merged in previous step)
        id: wait_merge
        if: steps.no_diff.outputs.data_changed == 'true' && steps.merge_now.outputs.merged_now == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          NUM="${{ steps.pr.outputs.pr_number }}"
          if [ -z "$NUM" ]; then
            echo "pr_merged=false" >> "$GITHUB_OUTPUT"
            echo "same_cycle_pending=false" >> "$GITHUB_OUTPUT"
            echo "[SAME-CYCLE] No PR number — skip wait"
            exit 0
          fi
          MAX_WAIT=480
          ELAPSED=0
          while [ "$ELAPSED" -lt "$MAX_WAIT" ]; do
            STATE=$(gh pr view "$NUM" --json state -q '.state' 2>/dev/null || true)
            if [ "$STATE" = "MERGED" ]; then
              echo "pr_merged=true" >> "$GITHUB_OUTPUT"
              echo "same_cycle_pending=false" >> "$GITHUB_OUTPUT"
              echo "[SAME-CYCLE] PR #$NUM merged after ${ELAPSED}s"
              exit 0
            fi
            sleep 15
            ELAPSED=$((ELAPSED + 15))
          done
          echo "pr_merged=false" >> "$GITHUB_OUTPUT"
          echo "same_cycle_pending=true" >> "$GITHUB_OUTPUT"
          echo "[SAME-CYCLE] Timeout waiting for merge — auto-merge still pending; workflow continues (no false-fail)"
          exit 0

      - name: Dispatch Pages deploy (merge via GITHUB_TOKEN may not trigger push → pages.yml)
        if: steps.no_diff.outputs.data_changed == 'true' && steps.commit_push.outputs.pushed == 'true' && (steps.merge_now.outputs.merged_now == 'true' || steps.wait_merge.outputs.pr_merged == 'true')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          gh workflow run pages.yml --ref main --repo "${{ github.repository }}"
          echo "[PAGES] Dispatched pages.yml after data PR merge"

      - name: Wait for Pages deploy and verify prod
        id: deploy_verify
        if: steps.no_diff.outputs.data_changed == 'true' && steps.commit_push.outputs.pushed == 'true' && (steps.merge_now.outputs.merged_now == 'true' || steps.wait_merge.outputs.pr_merged == 'true')
        run: |
          set -euo pipefail
          EXPECTED="${{ steps.capture_ts.outputs.generated_at }}"
          [ -z "$EXPECTED" ] && echo "[DEPLOY] No expected generatedAt, skip verify" && exit 0
          RUN_START=${GITHUB_RUN_STARTED_AT:-$(date -u +%Y-%m-%dT%H:%M:%SZ)}
          RUN_EPOCH=$(date -d "$RUN_START" +%s 2>/dev/null || date +%s)
          MAX_WAIT=600
          ELAPSED=0
          while [ "$ELAPSED" -lt "$MAX_WAIT" ]; do
            PROD_JSON=$(curl -sS -m 15 "https://infouzel.cz/projects/data/articles.json" 2>/dev/null || true)
            PROD_AT=$(echo "$PROD_JSON" | jq -r '.generatedAt // empty' 2>/dev/null || true)
            if [ -n "$PROD_AT" ] && [ "$PROD_AT" = "$EXPECTED" ]; then
              END_EPOCH=$(date +%s)
              LATENCY=$((END_EPOCH - RUN_EPOCH))
              echo "fetchToProdLatencySeconds=$LATENCY" >> "$GITHUB_OUTPUT"
              echo "[DEPLOY] Prod has build data. generatedAt=$PROD_AT latency_seconds=$LATENCY"
              exit 0
            fi
            echo "[DEPLOY] Poll $ELAPSED s: prod generatedAt=$PROD_AT (expected=$EXPECTED)"
            sleep 20
            ELAPSED=$((ELAPSED + 20))
          done
          echo "[DEPLOY] Timeout waiting for prod"
          exit 1

      - name: Deploy summary
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          echo "[DEPLOY] data_changed=${{ steps.no_diff.outputs.data_changed }}"
          echo "[DEPLOY] commit_skipped=${{ steps.no_diff.outputs.commit_skipped }}"
          echo "[DEPLOY] push_skipped=${{ steps.no_diff.outputs.push_skipped }}"
          echo "[DEPLOY] pr_skipped=${{ steps.no_diff.outputs.pr_skipped }}"
          echo "[DEPLOY] direct_push_to_main=false"
          MNO="${{ steps.merge_now.outputs.merged_now }}"
          PM="${{ steps.wait_merge.outputs.pr_merged }}"
          SCP="${{ steps.wait_merge.outputs.same_cycle_pending }}"
          if [ "$MNO" = "true" ] || [ "$PM" = "true" ]; then
            echo "prMergedSameCycle=true"
          else
            echo "prMergedSameCycle=false"
          fi
          if [ "$SCP" = "true" ]; then
            echo "sameCyclePending=true"
          else
            echo "sameCyclePending=false"
          fi
          DV="${{ steps.deploy_verify.outcome }}"
          if [ "$DV" = "success" ]; then
            echo "deployVerifyRan=true"
            echo "wasPublishedInSameCycle=true"
          else
            echo "deployVerifyRan=false"
            echo "wasPublishedInSameCycle=false"
          fi
          FT="${{ steps.deploy_verify.outputs.fetchToProdLatencySeconds }}"
          if [ -n "$FT" ]; then
            echo "fetchToProdLatencySeconds=$FT"
          fi

      - name: Notify completion (beep)
        if: always()
        run: printf '\a'
