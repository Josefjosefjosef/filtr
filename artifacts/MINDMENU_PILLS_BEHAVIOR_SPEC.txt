PIN_MEANING=persist label+url (no separate pinned/active state; "připnutí" = saved content survives reload)
EMPTY_URL_CLICK=open settings (trigger gear button for that pill so user can set URL)
LABEL_MAX_LEN=18
URL_EMPTY_ALLOWED=yes (stored as "")
